# model=claude condition=promptbook profile=busy task=T2

order_bottom_to_top = ['green block', 'red block']
say('Stacking all blocks with the red block on top')
stack_objects_in_order(object_names=order_bottom_to_top)
